package java9features;

public class C_JPMS {
    /** Java Plateform Module System
     * modularization (JPMS) ->
     *
     * in java9 tools.jar and rt.jar not present.
     * version conflicts are not there.
     * pack1 and pack2
     * Security problem
     * rt.jar are divided into smaller piece.
     *
     * All the dependent are present or not.
     *
     * */
}
