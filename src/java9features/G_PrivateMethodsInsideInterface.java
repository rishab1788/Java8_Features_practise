package java9features;
/**
 *
 *1.7 ->
 *
 *
 * 1.8
 * default methods -> without effecting implementation classes we can add new methods ( also override can happen)
 * static methods  ->
 *
 * 1.9
 * private methods we can take -> code reusebility ->
 *                               multiple default methods are present if deafault methods have common code than we can have
 *                                  private methods
 *
 *
 * */
public class G_PrivateMethodsInsideInterface {
}
